import { VehicleInUseDirective } from './vehicle-in-use.directive';

describe('VehicleInUseDirective', () => {
  it('should create an instance', () => {
    const directive = new VehicleInUseDirective();
    expect(directive).toBeTruthy();
  });
});
